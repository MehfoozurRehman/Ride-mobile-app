const express = require("express");

//api config
const app = express();
const port = process.env.PORT || 8800;

//middlewares

//db config

//api endpoints

//listners
app.listen(port, () => {
  console.log("server running");
});
